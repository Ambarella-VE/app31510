import { StyleSheet } from "react-native";

export const theme = StyleSheet.create({
  colors:{
    dark:{
      primary:'#e76b74',
      onSurface: '#ffffff',
      surface :'#585b56',
    }
  },
  text:{
    title:{
      large:{
        fontSize:22,
      }
    }
  }
});